{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C: gcc Build e rodar",
            "command": "./build.sh",
            "args": [
                "${file}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}